# Forgeway

A fresh, independent build — not a fork or port of `alloycompute`. New repo, new
history, no shared remote or deploy target with the old project.

Direction and scope are still being defined; this repo starts empty on purpose.
