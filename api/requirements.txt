# Dependency versions are declared in pyproject.toml — that's the single
# source of truth; this file only exists so `pip install -r requirements.txt`
# (documented in README.md) keeps working. It installs this package in
# editable mode with its `dev` extra (pytest, httpx), which also registers
# the `forgeway` console script.
-e .[dev]
